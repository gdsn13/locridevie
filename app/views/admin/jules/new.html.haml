- title t('.title')

- content_for :head do
  = include_javascripts :pages

- content_for :submenu do
  = render 'admin/shared/menu/pages'
    
%p!= t('.help')

= semantic_form_for @jule, :url => admin_jules_url, :html => { :multipart => true } do |form|

  = render 'form', :f => form

  = render 'admin/shared/form_actions', :back_url => admin_jules_url, :button_label => :create